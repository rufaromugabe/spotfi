# ============================================================================
# SpotFi - Cloud ISP Management Platform
# Environment Configuration
# ============================================================================
# Copy this file to .env and update with your actual values
# cp .env.example .env

# ============================================================================
# Node.js Environment
# ============================================================================
NODE_ENV=development

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
# Main database connection string for Prisma
# This same database is used for both application data and RADIUS accounting
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://spotfi:spotfi_password@localhost:5432/spotfi

# PostgreSQL settings (used by docker-compose)
POSTGRES_USER=spotfi
POSTGRES_PASSWORD=spotfi_password
POSTGRES_DB=spotfi_db

# ============================================================================
# JWT Authentication
# ============================================================================
# Secret key for signing JWT tokens
#  IMPORTANT: Change this to a strong random string in production!
# Generate one with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-secret-key-min-32-characters-long

# ============================================================================
# Server Configuration
# ============================================================================
# Port for the API server
PORT=8080

# Host to bind the server to (0.0.0.0 for all interfaces)
HOST=0.0.0.0

# CORS origin - allowed origins for cross-origin requests
# Use * for development, specify domains in production
CORS_ORIGIN=http://localhost:3000

# API URL for Swagger documentation (used in OpenAPI spec)
API_URL=http://localhost:8080

# ============================================================================
# Billing Configuration
# ============================================================================
# Cost per MB in USD for billing calculations
# Default: $0.02 per MB
BILLING_COST_PER_MB=0.02

# ============================================================================
# FreeRADIUS Configuration
# ============================================================================
# FreeRADIUS uses the same PostgreSQL database as the main application
# Database connection is automatically configured from POSTGRES_* variables
# No additional RADIUS database variables needed
