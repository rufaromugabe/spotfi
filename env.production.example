# SpotFi Production Environment Configuration

# Server Configuration
NODE_ENV=production
PORT=8080
HOST=0.0.0.0
CORS_ORIGIN=*
API_URL=http://20.253.179.225:8080

# Database Configuration (PostgreSQL)
POSTGRES_DB=spotfi
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
POSTGRES_PORT=5432

# JWT Secret (generate a secure random string: openssl rand -base64 64)
JWT_SECRET=CHANGE_ME_TO_SECURE_JWT_SECRET_MIN_32_CHARACTERS

# Database URL (auto-generated from above, but can be overridden)
# DATABASE_URL=postgresql://postgres:CHANGE_ME_TO_SECURE_PASSWORD@postgres:5432/spotfi?schema=public

# FreeRADIUS Configuration (handled automatically via docker-compose)
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=spotfi
# DB_USER=postgres
# DB_PASS=CHANGE_ME_TO_SECURE_PASSWORD
# DB_DIALECT=postgresql

