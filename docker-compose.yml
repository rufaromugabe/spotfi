version: '3.8'

services:
  freeradius:
    build:
      context: ./radius
      dockerfile: Dockerfile
    container_name: spotfi-radius
    environment:
      DB_HOST: 62.72.19.27
      DB_PORT: 5998
      DB_NAME: spotfi
      DB_USER: postgres
      DB_PASSWORD: passcode
      DB_DIALECT: postgresql
      PGPASSWORD: passcode
      PGUSER: postgres
      PGHOST: 62.72.19.27
      PGPORT: 5998
      PGDATABASE: spotfi
      RADIUS_SECRET: testing123
      RADIUS_DEBUG: no
    ports:
      - "1812:1812/udp"  # Authentication
      - "1813:1813/udp"  # Accounting
    networks:
      - spotfi-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  spotfi-network:
    driver: bridge

